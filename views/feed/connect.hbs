{{>navbar userInfo=userInfo}}
<h2>Find your friends</h2>
<form action="/feed/connect-search" class="border border-2 p-2 bg-light" method="GET">
<div class="form-group d-flex">
  <input type="text" class="form-control" id="text" aria-describedby="text" name="search" value="{{search}}" required>
  <button class="btn btn-light" type="submit">🔎</button>
 </div>
</form>

<div class="searchContainer">
    {{#each userFriends}}
        <a href="/profile/{{this._id}}" class="link-dark text-decoration-none"><img src="{{this.profilePhoto}}" width="25px" height="25px"> {{this.username}} - {{this.name}} {{this.surname}}</a> <a href="/feed/disconnect/{{this._id}}" class="btn btn-danger btn-sm">Unfollow</a><br>
    {{/each}}
    {{#each userNotFriends}}
        <a href="/profile/{{this._id}}" class="link-dark text-decoration-none"></a><img src="{{this.profilePhoto}}" width="25px" height="25px"> {{this.username}} - {{this.name}} {{this.surname}}</a> <a href="/feed/connect/{{this._id}}" class="btn btn-primary btn-sm">Follow</a><br>
    {{/each}}
</div>