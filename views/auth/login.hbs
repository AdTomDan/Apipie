
<div class="container d-flex justify-content-center position-absolute bg-dark" style="margin-top: 0%;">
  <img
    class="pie-img position-absolute" style="margin-top: -25%; width: 50%"
    src="/images/pie_image_login.png"
    alt="Cherry Pie Image"
  /></img>
</div>
<div
  class="container login-block text-center d-flex justify-content-center align-items-center h-100"
>

  <main class="form-signin">

    <form class="loginForm justify-content-center" action="/auth" method="POST">

      <a class="active" href="/"><img
          class="logo-img"
          src="/images/logo.png"
          alt="logo!"
        /></a>
      <div class="form-floating">
        <input
          class="form-control"
          id="floatingInput"
          type="text"
          name="username"
          placeholder="pepipalotes"
          required
        />
        <label for="floatingInput">Username</label>
      </div>
      <div class="form-floating">
        <input
          class="form-control"
          id="floatingPassword"
          type="password"
          name="password"
          id="password"
          placeholder="🔒"
          required
        />
        <label for="floatingPassword">Password</label>
      </div>
      <button class="w-100 btn btn-lg btn-primary" type="submit">Log in</button>
    </form>

    Hungry and without an account?
    <a href="/auth/signup">Sign up</a>

  </main>
</div>
{{#if errorMessage}}
  <p>{{errorMessage}}</p>
{{/if}}